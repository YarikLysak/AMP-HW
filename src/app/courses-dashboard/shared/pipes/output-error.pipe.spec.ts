import { OutputErrorPipe } from './output-error.pipe';

describe('OutputErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new OutputErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
